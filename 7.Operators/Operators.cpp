#include<iostream>

using namespace std;

int main()
{
    // Post fix and pre fix
    /*int a = 10;   
    
    cout<<"a "<<a<<endl;
    int b = a++; // post fix, a is incremented but inital value of a is stored in b
    // increment will occur after storing the value in b
    cout<<"b "<<b<<endl; //10
    cout<<"a "<<a<<endl; //11

    b = ++a;// prefix, a is incremented and stored in b
    cout<<"a "<<a<<endl; // 12
    cout<<"b "<<b; //12*/


    /*// input number and tell if its = or > or < 10
    int n;
    cin>>n;

    if(n==10)
        cout<<"Number is equal to 10";
    else if(n>10)
        cout<<"Number is greater that 10";
    else 
        cout<<"Number is less than 10";*/


    //write a prog to output whether a number is divisible by
    //both 2 and 3 or divisible only by one of them
    /*int n;
    cin>>n;

    if(n%2==0 && n%3==0)
        cout<<"Number is divisible by 2 and 3\n";    
    else if(n%2==0)
            cout<<"Number is divisible only by 2\n";
    else if(n%3==0)
            cout<<"Number is divisible only by 3\n";*/
    
    
 



}